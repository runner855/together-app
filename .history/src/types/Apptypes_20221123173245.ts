export enum AppUrls  {
    NETWORK = "/network",
    CREATEGROUP = "/create-group",
    
}

export enum LanguageEnum {
    IT =  "it",
    EN = "en"
}

const languages : LanguageEnum = languages.IT;

